pip install streamlit pydeck numpy pandas scikit-learn requests altair 
# If using docplex for LP:
pip install docplex
# If using pulp + cplex solver:
pip install pulp
# Then separately install or configure CPLEX if needed.
pip install python-dotenv
pip install plotly  
pip install matplotlib  